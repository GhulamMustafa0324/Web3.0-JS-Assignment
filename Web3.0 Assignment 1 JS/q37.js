function make_shirt(size = "large", message = "I love JavaScript") {
  console.log(
    `size of the shirt is ${size} and the message on the shirt is ${message}`
  );
}

make_shirt();
make_shirt("medium");
make_shirt("small", "Indeed, Allah is with the patient");
