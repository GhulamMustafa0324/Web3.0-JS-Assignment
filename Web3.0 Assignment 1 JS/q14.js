let relatives = ["GrandDad", "GrandMom", "Uncle", "Aunt"];

for (let i = 0; i < relatives.length; i++) {
  console.log(relatives[i], `would you like to come over for a dinner with my family?`);
}
